{
  "name": "home-bird",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp run -r --parallel dev",
    "build": "vp run -r build",
    "test": "vp test",
    "test:unit": "vp test --project unit",
    "test:integration": "vp test --project integration",
    "lint": "vp lint",
    "fmt": "vp fmt",
    "check": "vp check",
    "typecheck": "vp run -r typecheck",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:push": "pnpm --filter @home-bird/database db:push",
    "db:generate": "pnpm --filter @home-bird/database db:generate",
    "db:migrate": "pnpm --filter @home-bird/database db:migrate",
    "db:studio": "pnpm --filter @home-bird/database db:studio"
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=26.0.0"
  },
  "packageManager": "pnpm@11.17.0"
}
